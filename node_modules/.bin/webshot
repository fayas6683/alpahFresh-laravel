#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../webshot-cli/webshot" "$@"
  ret=$?
else 
  node  "$basedir/../webshot-cli/webshot" "$@"
  ret=$?
fi
exit $ret
